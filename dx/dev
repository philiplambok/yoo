#!/bin/bash
# Start Rails development server with proper binding

echo "🚂 Starting Rails development server..."
echo "📡 Server will be accessible at http://localhost:3000"
echo "🔄 Press Ctrl+C to stop"
echo ""

# Start the Rails server bound to all interfaces
docker-compose -f docker-compose.dev.yml exec app bundle exec rails server -b 0.0.0.0