services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    init: true
    volumes:
      - type: bind
        source: .
        target: /root/yoo
    ports:
      - "3000:3000"
    environment:
      - RAILS_ENV=development
    stdin_open: true
    tty: true